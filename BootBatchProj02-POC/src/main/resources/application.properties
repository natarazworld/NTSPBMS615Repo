# create db tables in underlying DB s/w having  JobRepository Recorded information
spring.batch.jdbc.initialize-schema=always
#enable batch processing on the application startup or when  launcher.run(-,-) is called
spring.batch.job.enabled=true
